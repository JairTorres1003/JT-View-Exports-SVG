{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "JSON schema for i18n in View Exports SVG (client) extension for Visual Studio Code",
  "type": "object",
  "properties": {
    "labels": {
      "type": "object",
      "properties": {
        "Clear": {
          "type": "string",
          "default": "Clear"
        },
        "Close": {
          "type": "string",
          "default": "Close"
        },
        "Search": {
          "type": "string",
          "default": "Search"
        },
        "Select color": {
          "type": "string",
          "default": "Select color"
        },
        "Copy code": {
          "type": "string",
          "default": "Copy code"
        },
        "Copied {{value}} to clipboard": {
          "type": "string",
          "default": "Copied <b>{{value}}</b> to clipboard"
        },
        "Reset code": {
          "type": "string",
          "default": "Reset code"
        },
        "Show code": {
          "type": "string",
          "default": "Show code"
        },
        "Hide code": {
          "type": "string",
          "default": "Hide code"
        },
        "Loading": {
          "type": "string",
          "default": "Loading..."
        },
        "Extracting components": {
          "type": "string",
          "default": "Extracting components..."
        }
      },
      "required": [
        "Clear",
        "Close",
        "Search",
        "Select color",
        "Copy code",
        "Copied {{value}} to clipboard",
        "Reset code",
        "Show code",
        "Hide code",
        "Loading",
        "Extracting components"
      ]
    },
    "DevTools": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "default": "DevTools"
        }
      },
      "required": ["title"]
    },
    "errors": {
      "type": "object",
      "properties": {
        "VSCode API is not available": {
          "type": "string",
          "default": "VSCode API is not available"
        }
      },
      "required": ["VSCode API is not available"]
    }
  },
  "required": ["labels", "errors"]
}
