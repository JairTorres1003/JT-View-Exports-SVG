import { Box, styled } from '@mui/material'

export const BoxDevTools = styled(Box, { name: 'DevTools-Box' })(() => ({
  width: '100%',
  minWidth: 170,
  maxWidth: '100%',
  height: '100%',
  padding: '16px',
}))
