{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build:extension-api",
      "type": "shell",
      "command": "pnpm build:extension-api",
      "group": "build",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
